---
id: 07-02-run-verification-and-triage
phase: 07-reconciliation
plan: 02
type: execute
wave: 1
depends_on:
  - 07-01-verifier-wrap
files_modified:
  - ".planning/reconciliation/elexon/"                                                                   # new dir + per-finding markdown files
  - ".planning/reconciliation/entsoe/"
  - ".planning/reconciliation/entsog/"
  - ".planning/reconciliation/gie/"
  - ".planning/reconciliation/neso/"
  - ".planning/reconciliation/openmeteo/"
  - ".planning/phases/07-reconciliation/07-02-VERIFICATION-REPORT.md"                                    # human-readable run notes
  - "C:/Users/Bobbo/OneDrive/Desktop/Learning/AI/quant-vault/30-vendors/vault-curl-schema-validation.json"  # regenerated by the verifier
  - "C:/Users/Bobbo/OneDrive/Desktop/Learning/AI/quant-vault/30-vendors/vault-curl-schema-validation.md"   # regenerated by the verifier
requirements:
  - RECON-02
autonomous: true
must_haves:
  truths:
    - "Verification runs across all 6 Vendors in a single execution (Elexon, ENTSO-E, ENTSO-G, GIE, NESO, Open-Meteo) — per D-04 (all-Vendor upfront)"
    - "Every Drift instance the verifier surfaces has a corresponding finding file under `.planning/reconciliation/<vendor>/<NN>-<slug>.md`"
    - "Every finding file's YAML frontmatter contains `status:` (`open`, `wontfix`, or `needs-info`); finding files where applicable carry `reason:` (`v3-candidate` or `defer-entitlement`)"
    - "The 5 load-bearing examples from DRIFT-SURFACES.md (§§ 4.1, 4.2, 4.4, 4.5, 4.6) all surface as finding files in the appropriate Vendor directory — the acceptance gate"
    - "The 22 Elexon `manual_transformer_schema` cases each have a `wontfix`/`v3-candidate` finding file (one per slug listed in 07-CONTEXT.md <specifics>)"
    - "The 33 ENTSO-E HTTP 401 cases each have a `needs-info`/`defer-entitlement` finding file pointing at DRIFT-SURFACES § 4.4 and Q-DD-04"
    - "JSON and markdown verifier reports regenerate (existing JSON keys preserved; additive extensions per Q-DD-17 are allowed)"
  artifacts:
    - path: ".planning/reconciliation/elexon/"
      provides: "Per-finding markdown files for Elexon (open + wontfix-v3 buckets)"
      contains_at_least: 25     # 3 published_at/issue_time findings + 22 manual_transformer_schema = 25; plus any 18 ENTSO-E nullability findings actually filed under elexon? — those are ENTSO-E findings, see entsoe/
    - path: ".planning/reconciliation/entsoe/"
      provides: "Per-finding markdown files for ENTSO-E (33 needs-info entitlement findings + 18 open nullability findings)"
      contains_at_least: 33     # the 33 401 cases minimum; 18 nullability findings additional
    - path: ".planning/reconciliation/entsog/"
      provides: "Per-finding markdown files for ENTSO-G (4 404 endpoints + physical_flows 35-field rewrite)"
      contains_at_least: 5
    - path: ".planning/reconciliation/gie/"
      provides: "Per-finding markdown files for GIE (whatever Verification surfaces — 0 known load-bearing)"
      contains_at_least: 0
    - path: ".planning/reconciliation/neso/"
      provides: "Per-finding markdown files for NESO (whatever Verification surfaces — 0 known load-bearing)"
      contains_at_least: 0
    - path: ".planning/reconciliation/openmeteo/"
      provides: "Per-finding markdown files for Open-Meteo (whatever Verification surfaces — 0 known load-bearing)"
      contains_at_least: 0
    - path: ".planning/phases/07-reconciliation/07-02-VERIFICATION-REPORT.md"
      provides: "Human-readable run notes: when the verifier ran, per-Vendor counts, the 5 load-bearing checks explicitly recorded"
      contains: "Acceptance gate"
  key_links:
    - from: "Verifier JSON output `vault-curl-schema-validation.json`"
      to: ".planning/reconciliation/<vendor>/<NN>-<slug>.md"
      via: "manual triage walk-through using the `triage` skill state-machine"
      pattern: "Reference: vault-curl-schema-validation.md L\\d+"
    - from: "Every `wontfix`/`v3-candidate` finding file"
      to: "PROJECT.md § Out of Scope"
      via: "explicit citation in the finding body"
      pattern: "PROJECT\\.md § Out of Scope"
    - from: "Every `needs-info`/`defer-entitlement` finding file"
      to: "DRIFT-SURFACES.md § 4.4 + Q-DD-04"
      via: "explicit citation in the finding body"
      pattern: "DRIFT-SURFACES\\.md § 4\\.4|Q-DD-04"
---

<objective>
Run `gridflow-drift-check` (produced in 07-01) against all 6 Vendors in a single execution; consume the regenerated JSON + markdown reports; emit per-finding markdown files under `.planning/reconciliation/<vendor>/<NN>-<slug>.md` for every Drift instance Verification surfaces; triage each finding into one of three buckets — `open`, `wontfix`/`v3-candidate`, `needs-info`/`defer-entitlement` — using the `triage` skill state-machine.

This plan is the **acceptance gate** for RECON-02: the 5 load-bearing examples from DRIFT-SURFACES.md (§§ 4.1, 4.2, 4.4, 4.5, 4.6) MUST all surface as finding files; missing any of them means the verifier wiring is wrong and 07-01 needs revisiting.

Purpose: produce the structured triage input that 07-03 will consume to land Vault edits (the `open` bucket) and that Phase 9 will consume to make the ENTSO-E entitlement decision (the `needs-info`/`defer-entitlement` bucket).

Output: 6 Vendor subdirectories under `.planning/reconciliation/` populated with finding markdown files; a human-readable run report at `.planning/phases/07-reconciliation/07-02-VERIFICATION-REPORT.md`; regenerated `vault-curl-schema-validation.{json,md}` in the upstream Vault.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-reconciliation/07-CONTEXT.md
@.planning/phases/07-reconciliation/07-01-SUMMARY.md
@.planning/research/post-v1/drift-detection/DRIFT-SURFACES.md
@docs/agents/issue-tracker.md
@docs/agents/triage-labels.md
@CONTEXT.md

<finding_file_template>
<!-- This is the exact template every finding file uses. The `triage` skill      -->
<!-- and `to-issues` skill produce content that fits this shape.                 -->

```markdown
---
status: <open | wontfix | needs-info>
reason: <v3-candidate | defer-entitlement>     # required when status != open
created: 2026-05-19
vendor: <elexon | entsoe | entsog | gie | neso | openmeteo>
dataset: <slug>                                # e.g. ndf, fuelhh, physical_flows
drift_category: <Structural | Semantic | Referential | Temporal | Volumetric>
verifier_finding_id: <line range in vault-curl-schema-validation.md>
references:
  - DRIFT-SURFACES.md § <N.M>                  # cite the surface example
  - vault-curl-schema-validation.md L<from>-L<to>
tags: [drift]
---

# <Vendor> · <dataset>: <one-line title>

## Context

<What disagrees with what. Which layer wins per CONTEXT.md trust chain
(Live API → Canonical → Vault → Site). For `wontfix`/`v3-candidate` cite
PROJECT.md § Out of Scope. For `needs-info`/`defer-entitlement` cite
DRIFT-SURFACES.md § 4.4 + Q-DD-04.>

## Acceptance

<What "closed" looks like. For `open`: the exact Vault edit needed.
For `wontfix`: documented and not actioned.
For `needs-info`: Phase 9 discuss-phase makes the call.>

## Comments

<empty until 07-03 (for `open`) or Phase 9 (for `needs-info`) acts on it>
```
</finding_file_template>

<load_bearing_acceptance>
<!-- The 5 examples that MUST surface as finding files. Missing ANY of these     -->
<!-- means the verifier wiring is wrong (acceptance gate per RECON-02).          -->

| DRIFT-SURFACES § | Vendor    | Dataset(s)            | Status            | Reason          |
|------------------|-----------|-----------------------|-------------------|-----------------|
| § 4.1            | elexon    | ndf, ndfd             | open              | (none — fixable)|
| § 4.2            | elexon    | fuelhh                | open              | (none — fixable)|
| § 4.4            | entsoe    | (33 HTTP 401 datasets)| needs-info        | defer-entitlement |
| § 4.5            | entsog    | hydrogen_content, interruptions, methane_content, oxygen_content | open | (none — fixable: delete or `removed:` flag) |
| § 4.6            | entsog    | physical_flows        | open              | (none — fixable: schema-table rewrite) |
</load_bearing_acceptance>

<wontfix_v3_catalogue>
<!-- The 22 Elexon manual_transformer_schema slugs from 07-CONTEXT.md <specifics>. -->
<!-- Each gets a finding file with status=wontfix, reason=v3-candidate.            -->

```
agpt, agws, atl, fou2t14d, fuelinst, imbalngc, inddem, indgen, indo, indod,
itsdo, lolpdrm, market_depth, melngc, netbsad, nonbm, remit, soso, temp,
tsdf, tsdfd, uou2t14d
```

Each finding cites: PROJECT.md § Out of Scope + DRIFT-SURFACES.md § 4.3 (or § 3 Referential) + ADR-0001 Consequences ("declaring `ElexonAGPT`, `ElexonAGWS`, etc. cross-repo work").
</wontfix_v3_catalogue>

<entsoe_401_catalogue>
<!-- The 33 ENTSO-E HTTP 401 cases per DRIFT-SURFACES § 4.4.                      -->
<!-- The exact slug list comes from the verifier's regenerated report — read     -->
<!-- vault-curl-schema-validation.md after Task 1 runs to enumerate them.        -->

Each finding cites: DRIFT-SURFACES.md § 4.4 + Q-DD-04 + 07-CONTEXT.md D-06.

Body content: "Token works for 11 ENTSO-E endpoints (vault-amendment-plan.md:60-71); not this one. Resolution path deferred to Phase 9 discuss-phase per D-06: extend access vs `skip-with-warn`."
</entsoe_401_catalogue>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute `gridflow-drift-check` across all 6 Vendors; regenerate JSON + markdown reports</name>
  <files>
    - C:/Users/Bobbo/OneDrive/Desktop/Learning/AI/quant-vault/30-vendors/vault-curl-schema-validation.json (regenerated)
    - C:/Users/Bobbo/OneDrive/Desktop/Learning/AI/quant-vault/30-vendors/vault-curl-schema-validation.md (regenerated)
    - .planning/phases/07-reconciliation/07-02-VERIFICATION-REPORT.md (new)
  </files>
  <read_first>
    - .planning/phases/07-reconciliation/07-01-SUMMARY.md (confirm `gridflow-drift-check` is installed and resolves)
    - C:/Users/Bobbo/OneDrive/Desktop/Learning/AI/quant-vault/30-vendors/vault-curl-schema-validation.md (the most recent run output as the baseline — 2026-05-16T15:34:03Z)
    - C:/Users/Bobbo/OneDrive/Desktop/Learning/AI/quant-vault/30-vendors/vault-curl-schema-validation.json (baseline machine output)
    - C:/Users/Bobbo/OneDrive/Desktop/Learning/AI/quant-vault/30-vendors/scripts/gridflow_drift_check.py (read the `main()` function to confirm CLI argument shape — does it iterate all Vendors by default? what flags does it accept?)
    - .planning/phases/07-reconciliation/07-CONTEXT.md (D-04: all-Vendor upfront in one execution)
  </read_first>
  <action>
    Step 1 — Confirm the renamed script's CLI surface. Read `gridflow_drift_check.py` end-to-end if you haven't already. The script enumerates Vendor directories from the vault root (`VENDOR_VAULT_DIR`); it iterates everything it finds. There is no per-Vendor flag — the existing behaviour IS "all Vendors at once" (D-04 satisfied by default).

    Step 2 — Prepare the environment.
    ```bash
    # Ensure the .env file has API keys for ENTSOE and GIE.
    # The 33 ENTSO-E 401 cases per DRIFT-SURFACES § 4.4 are EXPECTED — we want them
    # to surface as findings, not be skipped silently. The verifier's existing
    # skip-with-warn semantic at scripts/verify_curl_and_silver_schema.py:158-173
    # records 401 as `failed_auth`, which IS what we want.

    test -f "C:/Users/Bobbo/OneDrive/Desktop/Learning/AI/quant-vault/30-vendors/.env" || \
      cp "C:/Users/Bobbo/OneDrive/Desktop/Python/gridflow/.env" \
         "C:/Users/Bobbo/OneDrive/Desktop/Learning/AI/quant-vault/30-vendors/.env"
    # (Skip the cp if .env already exists; the script uses GRIDFLOW_ENV_FILE override.)
    ```

    Step 3 — Run the verifier. Use the console script installed in 07-01.
    ```bash
    cd "C:/Users/Bobbo/OneDrive/Desktop/Learning/AI/quant-vault/30-vendors"
    gridflow-drift-check 2>&1 | tee /tmp/drift-check-run.log
    ```
    Expected runtime: ~5-10 minutes (the script makes ~150-200 live curl calls with per-Vendor sleep). Save the full stdout/stderr log — it's evidence for SUMMARY.md.

    The run regenerates `vault-curl-schema-validation.json` and `vault-curl-schema-validation.md` in-place inside `quant-vault/30-vendors/`. Per Q-DD-17 default lean, these stay committed in the Vault as historical record (07-04 pushes them with the rest).

    Step 4 — Produce the run report at `.planning/phases/07-reconciliation/07-02-VERIFICATION-REPORT.md`. Required sections:
    ```markdown
    # 07-02 Verification Run Report

    **Ran:** <ISO timestamp from vault-curl-schema-validation.md line 3>
    **Verifier:** gridflow-drift-check (renamed from verify_curl_and_silver_schema.py in 07-01)

    ## Per-Vendor summary

    | Vendor      | Curl OK | Curl 401/`failed_auth` | Curl other failed | Silver OK | Silver failed | manual_transformer_schema | no_silver_schema_table |
    |-------------|---------|------------------------|-------------------|-----------|---------------|---------------------------|------------------------|
    | elexon      | <n>     | <n>                    | <n>               | <n>       | <n>           | <n>                       | <n>                    |
    | entsoe      | <n>     | <n>                    | <n>               | <n>       | <n>           | <n>                       | <n>                    |
    | entsog      | <n>     | <n>                    | <n>               | <n>       | <n>           | <n>                       | <n>                    |
    | gie         | <n>     | <n>                    | <n>               | <n>       | <n>           | <n>                       | <n>                    |
    | neso        | <n>     | <n>                    | <n>               | <n>       | <n>           | <n>                       | <n>                    |
    | openmeteo   | <n>     | <n>                    | <n>               | <n>       | <n>           | <n>                       | <n>                    |
    | **Total**   | <n>     | <n>                    | <n>               | <n>       | <n>           | <n>                       | <n>                    |

    Cross-check against vault-curl-schema-validation.md § Summary — totals must match.

    ## Load-bearing acceptance gate (RECON-02)

    The 5 examples below MUST appear in the regenerated report. Tick each:

    - [ ] DRIFT-SURFACES § 4.1 — ndf/ndfd `published_at` Structural+Semantic Drift surfaces in `elexon` silver-schema failures (look for `Missing in docs: published_at` in vault-curl-schema-validation.md under `elexon\\datasets\\ndf.md` and `ndfd.md`)
    - [ ] DRIFT-SURFACES § 4.2 — fuelhh `published_at` Structural Drift surfaces in `elexon` silver-schema failures (look for `Missing in docs: published_at` under `elexon\\datasets\\fuelhh.md`)
    - [ ] DRIFT-SURFACES § 4.4 — 33 ENTSO-E HTTP 401 cases surface (count `failed_auth` entries under `entsoe\\` in vault-curl-schema-validation.md; must be 33)
    - [ ] DRIFT-SURFACES § 4.5 — 4 ENTSO-G 404 endpoints surface (`hydrogen_content`, `interruptions`, `methane_content`, `oxygen_content` under `entsog\\datasets\\` returning HTTP 404)
    - [ ] DRIFT-SURFACES § 4.6 — ENTSO-G `physical_flows` 35-field mismatch surfaces (look for `Missing in docs: flow_gwh_per_day, timestamp_utc` and `Extra in docs: booking_platform_key, ...` under `entsog\\datasets\\physical_flows.md`)

    **If ANY of the above is unticked: STOP, file a blocker note, and route back to 07-01 — the wiring is wrong.**

    ## JSON schema extensions used in this run

    <List any additive keys beyond the v1 schema, per Q-DD-17. None expected unless the planner explicitly added per-Vendor totals or version stamps — document if so.>

    ## Notes

    <Anomalies, vendor 5xx flakes, env quirks. Anything 07-03 needs to know.>
    ```

    Do NOT proceed to Task 2 until every box in the acceptance gate is ticked.

    Conventional Commit message: `chore(07-02): re-run gridflow-drift-check across 6 Vendors and capture report`

    Note that the verifier regenerates the JSON/md reports in `quant-vault`, not in `gridflow-front-end`. The commit in this repo (`gridflow-front-end`) includes only `07-02-VERIFICATION-REPORT.md`. The upstream Vault commits to the regenerated reports are landed in 07-04 (where the new private GitHub repo gets initialised).
  </action>
  <verify>
    <automated>cd "C:/Users/Bobbo/OneDrive/Desktop/Learning/AI/quant-vault/30-vendors" && test -f vault-curl-schema-validation.md && test -f vault-curl-schema-validation.json && grep -c 'failed_auth' vault-curl-schema-validation.md ; grep -c 'Missing in docs: published_at' vault-curl-schema-validation.md ; grep -nE 'physical_flows\\.md' vault-curl-schema-validation.md | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `vault-curl-schema-validation.md`'s top-of-file timestamp is later than `2026-05-16T15:34:03.495442Z` (the baseline) — i.e. the verifier actually re-ran.
    - `grep -c 'failed_auth' vault-curl-schema-validation.md` returns at least 33 (the ENTSO-E entitlement cases; baseline shows 33 — equality or higher is acceptable, lower means we lost entitlement and 07-CONTEXT.md D-06 still applies).
    - `grep -c 'Missing in docs: published_at' vault-curl-schema-validation.md` returns at least 3 (ndf, ndfd, fuelhh — baseline confirmed via DRIFT-SURFACES §§ 4.1, 4.2).
    - `grep -nE 'entsog\\\\datasets\\\\physical_flows\\.md' vault-curl-schema-validation.md` returns at least one hit AND the surrounding lines mention `Missing in docs: flow_gwh_per_day, timestamp_utc` (§ 4.6 acceptance).
    - `grep -cE 'entsog\\\\datasets\\\\(hydrogen_content|interruptions|methane_content|oxygen_content)\\.md' vault-curl-schema-validation.md` returns at least 4 (§ 4.5 acceptance).
    - `test -f .planning/phases/07-reconciliation/07-02-VERIFICATION-REPORT.md` succeeds AND every checkbox under "Load-bearing acceptance gate" is ticked (`grep -c '- \\[ \\]' .planning/phases/07-reconciliation/07-02-VERIFICATION-REPORT.md` returns 0; `grep -c '- \\[x\\]' ...` returns 5).
  </acceptance_criteria>
  <done>
    Verifier regenerated JSON + markdown reports; the 5 load-bearing examples from DRIFT-SURFACES.md are all visible in the report; the run report is committed in this repo with every acceptance-gate checkbox ticked.
  </done>
</task>

<task type="auto">
  <name>Task 2: Emit per-finding markdown files for every Drift instance; triage into `open` / `wontfix-v3` / `needs-info`-`defer-entitlement` buckets</name>
  <files>
    - .planning/reconciliation/elexon/01-ndf-schema-table-missing-published-at.md (and ~24 siblings)
    - .planning/reconciliation/elexon/04-fuelhh-schema-table-missing-published-at.md
    - .planning/reconciliation/elexon/05-agpt-manual-transformer-schema.md … through 26-uou2t14d-manual-transformer-schema.md (22 wontfix-v3 findings)
    - .planning/reconciliation/entsoe/01-actual-generation-http-401.md (and 32 siblings — 33 total needs-info)
    - .planning/reconciliation/entsoe/34-resolution-field-nullability.md … (up to 51 — 18 open nullability findings; exact count from the regenerated report)
    - .planning/reconciliation/entsog/01-hydrogen-content-http-404.md
    - .planning/reconciliation/entsog/02-interruptions-http-404.md
    - .planning/reconciliation/entsog/03-methane-content-http-404.md
    - .planning/reconciliation/entsog/04-oxygen-content-http-404.md
    - .planning/reconciliation/entsog/05-physical-flows-schema-table-rewrite.md
    - .planning/reconciliation/gie/ (whatever Verification surfaces; 0 known)
    - .planning/reconciliation/neso/ (whatever Verification surfaces; 0 known)
    - .planning/reconciliation/openmeteo/ (whatever Verification surfaces; 0 known)
  </files>
  <read_first>
    - C:/Users/Bobbo/OneDrive/Desktop/Learning/AI/quant-vault/30-vendors/vault-curl-schema-validation.md (the regenerated report from Task 1 — the source list of findings)
    - C:/Users/Bobbo/OneDrive/Desktop/Learning/AI/quant-vault/30-vendors/vault-curl-schema-validation.json (machine-readable counterpart, used for any structured iteration)
    - C:/Users/Bobbo/OneDrive/Desktop/Learning/AI/quant-vault/30-vendors/vault-amendment-plan.md (informative: the Vault-author's existing fix list)
    - docs/agents/issue-tracker.md (the local-md tracker template — D-08)
    - docs/agents/triage-labels.md (canonical labels — `open` / `wontfix` / `needs-info`)
    - .planning/phases/07-reconciliation/07-CONTEXT.md (D-06 entitlement deferral; <specifics> the load-bearing catalogue + 22 wontfix-v3 slugs)
    - .planning/research/post-v1/drift-detection/DRIFT-SURFACES.md (§ 4.1-4.6 — finding bodies cite these)
    - CONTEXT.md (project-root: Vendor / Drift / Reconciliation vocabulary — used in finding bodies)
  </read_first>
  <action>
    Walk the regenerated `vault-curl-schema-validation.md` top-to-bottom and emit one finding file per Drift instance. Per D-07, use the `triage` skill state-machine to walk findings through `needs-triage` → final-status transitions; per D-08, findings live as local markdown using the template from `docs/agents/issue-tracker.md`.

    **Per-Vendor sequencing.** `<NN>` is per-Vendor (zero-padded two-digit, starts at `01`). Slug is kebab-case describing the finding. Example layout for Elexon:
    ```
    .planning/reconciliation/elexon/
      01-ndf-schema-table-missing-published-at.md           # status: open
      02-ndfd-schema-table-missing-published-at.md          # status: open
      03-ndf-national-demand-nullability-mismatch.md        # status: open  (DRIFT-SURFACES § 4.1 Semantic part)
      04-fuelhh-schema-table-missing-published-at.md        # status: open  (§ 4.2)
      05-agpt-manual-transformer-schema.md                  # status: wontfix · reason: v3-candidate
      06-agws-manual-transformer-schema.md                  # status: wontfix · reason: v3-candidate
      ...
      26-uou2t14d-manual-transformer-schema.md              # status: wontfix · reason: v3-candidate
    ```

    **Triage matrix (apply mechanically; the `triage` skill walks each candidate through this):**

    | Verifier symptom                                                     | Vendor    | status      | reason            | Notes                                                                 |
    |----------------------------------------------------------------------|-----------|-------------|-------------------|-----------------------------------------------------------------------|
    | `Missing in docs: published_at` on ndf/ndfd/fuelhh                   | elexon    | open        | (none)            | 07-03 adds the row. § 4.1 / § 4.2                                     |
    | `Nullable mismatch: national_demand_mw` on ndf                       | elexon    | open        | (none)            | § 4.1 Semantic part                                                   |
    | `manual_transformer_schema` for the 22 slugs in wontfix_v3_catalogue | elexon    | wontfix     | v3-candidate      | Cite PROJECT.md § Out of Scope + ADR-0001 Consequences. One file each |
    | Other `Missing in docs:` / `Extra in docs:` / `Nullable mismatch:`   | elexon    | open        | (none)            | Should be rare beyond the above; default to `open` if fixable in Vault|
    | HTTP 401 on any endpoint                                             | entsoe    | needs-info  | defer-entitlement | Cite DRIFT-SURFACES § 4.4 + Q-DD-04 + D-06                            |
    | `Nullable mismatch: resolution` on any ENTSO-E dataset               | entsoe    | open        | (none)            | 07-03 adds Nullable=Yes to 18 ENTSO-E tables (07-CONTEXT.md <specifics>) |
    | HTTP 404 on hydrogen_content/interruptions/methane_content/oxygen_content | entsog | open      | (none)            | § 4.5 — 07-03 deletes or marks `removed:`                              |
    | `Missing in docs: flow_gwh_per_day, timestamp_utc` on physical_flows | entsog    | open        | (none)            | § 4.6 — 07-03 rewrites schema table                                   |
    | Any other surfaced Drift on entsog / gie / neso / openmeteo          | (vendor)  | open        | (none)            | Default to `open` if fixable in Vault; only `wontfix-v3` if it needs Canonical changes |
    | manual_transformer_schema cases on ENTSO-G / GIE / NESO / Open-Meteo | (vendor)  | wontfix     | v3-candidate      | Same rationale as Elexon — cross-repo work                            |

    **One finding per disagreement.** If `vault-curl-schema-validation.md` lists `Missing in docs: published_at · Nullable mismatch: national_demand_mw` for `ndf.md` as a single failure block, that splits into TWO finding files (one Structural for `published_at`, one Semantic for `national_demand_mw`) — they have different fix paths and the per-finding granularity matters for 07-03 to walk them independently.

    **Body content per finding** — fill in the `## Context` and `## Acceptance` sections according to the template (in <finding_file_template> above). For `open` findings, `## Acceptance` MUST describe the exact Vault edit needed (file path + content), so 07-03 can act mechanically. For `wontfix`/`v3-candidate`, `## Acceptance` MUST say "documented and not actioned in v2; closes when `ElexonXXX` Pydantic class lands in `gridflow.schemas.elexon` per v3". For `needs-info`/`defer-entitlement`, `## Acceptance` MUST say "Phase 9 discuss-phase decides extend-access vs `skip-with-warn` per D-06".

    **Finding-file count sanity check.** After all files are written:
    - `.planning/reconciliation/elexon/` should have at least 4 `open` findings (ndf published_at, ndfd published_at, ndf nullability, fuelhh published_at) plus 22 `wontfix-v3` findings (one per `manual_transformer_schema` slug) = 26 minimum. Higher is fine.
    - `.planning/reconciliation/entsoe/` should have at least 33 `needs-info`/`defer-entitlement` findings (one per 401) plus 18 `open` nullability findings = 51 minimum.
    - `.planning/reconciliation/entsog/` should have at least 5 findings (4 × 404 + 1 × physical_flows rewrite). If the regenerated report surfaces more (e.g. `manual_transformer_schema` cases on ENTSO-G), those add to the count.
    - `.planning/reconciliation/gie/`, `.../neso/`, `.../openmeteo/` may be empty if Verification surfaces nothing; that's acceptable but the directory MUST exist (touch a `.gitkeep` if needed) so 07-03 / Phase 9 / Phase 10 don't trip on missing paths.

    **Per-finding commit cadence.** Don't open one giant commit. Commit per Vendor or per logical chunk:
    - `feat(07-02): elexon reconciliation findings (4 open + 22 wontfix-v3)`
    - `feat(07-02): entsoe reconciliation findings (33 defer-entitlement + 18 open)`
    - `feat(07-02): entsog reconciliation findings (5 open)`
    - `feat(07-02): gie/neso/openmeteo reconciliation findings (0 or more)`
    Each commit is one concern per CLAUDE.md.

    **Update the run report.** After all findings are written, append a `## Findings emitted` section to `07-02-VERIFICATION-REPORT.md` listing per-Vendor counts.
  </action>
  <verify>
    <automated>for v in elexon entsoe entsog gie neso openmeteo ; do echo "== $v ==" ; ls ".planning/reconciliation/$v/"*.md 2>/dev/null | wc -l ; done ; echo "== load-bearing acceptance ==" ; test -f .planning/reconciliation/elexon/01-ndf-schema-table-missing-published-at.md || echo "MISSING § 4.1 ndf" ; test -f .planning/reconciliation/elexon/04-fuelhh-schema-table-missing-published-at.md || echo "MISSING § 4.2 fuelhh" ; test -f .planning/reconciliation/entsog/05-physical-flows-schema-table-rewrite.md || echo "MISSING § 4.6 physical_flows" ; grep -l 'reason: defer-entitlement' .planning/reconciliation/entsoe/*.md 2>/dev/null | wc -l ; grep -l 'reason: v3-candidate' .planning/reconciliation/elexon/*.md 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - **Load-bearing acceptance gate (RECON-02 hard requirement):**
      - `.planning/reconciliation/elexon/01-ndf-schema-table-missing-published-at.md` exists with `status: open` (§ 4.1)
      - `.planning/reconciliation/elexon/02-ndfd-schema-table-missing-published-at.md` (or numbered differently — at minimum some elexon file references `ndfd` + `published_at`) exists with `status: open` (§ 4.1)
      - At least one `.planning/reconciliation/elexon/*.md` references `fuelhh` + `Missing in docs: published_at` with `status: open` (§ 4.2)
      - `.planning/reconciliation/entsog/*.md` includes one file per `hydrogen_content`, `interruptions`, `methane_content`, `oxygen_content` with HTTP 404 + `status: open` (§ 4.5)
      - At least one `.planning/reconciliation/entsog/*.md` references `physical_flows` + schema-table rewrite + `status: open` (§ 4.6)
      - `grep -l 'reason: defer-entitlement' .planning/reconciliation/entsoe/*.md | wc -l` returns at least 33 (§ 4.4)
    - **Triage-label fidelity** (cross-checked against `docs/agents/triage-labels.md`):
      - `grep -hE '^status:' .planning/reconciliation/*/*.md | sort -u` returns ONLY `status: open`, `status: wontfix`, `status: needs-info` (no other label values). The verifier output uses `open` as the default per `docs/agents/issue-tracker.md`.
      - `grep -hE '^reason:' .planning/reconciliation/*/*.md | sort -u` returns ONLY `reason: v3-candidate` or `reason: defer-entitlement` (no other reasons).
      - Every `status: wontfix` finding has a `reason: v3-candidate` (verified by: `awk '/^status: wontfix/{flag=1;next} flag&&/^reason:/{print;flag=0}' .planning/reconciliation/*/*.md | grep -v 'v3-candidate' | wc -l` returns 0).
      - Every `status: needs-info` finding has a `reason: defer-entitlement`.
    - **manual_transformer_schema wontfix coverage** (the 22 Elexon slugs from <wontfix_v3_catalogue>):
      - For each slug in (agpt, agws, atl, fou2t14d, fuelinst, imbalngc, inddem, indgen, indo, indod, itsdo, lolpdrm, market_depth, melngc, netbsad, nonbm, remit, soso, temp, tsdf, tsdfd, uou2t14d): `grep -l "dataset: $slug" .planning/reconciliation/elexon/*.md` returns at least one match AND that file has `status: wontfix` + `reason: v3-candidate`.
    - **Citation discipline:**
      - Every `status: wontfix` finding's body contains the literal string `PROJECT.md § Out of Scope`.
      - Every `status: needs-info` finding's body contains either `DRIFT-SURFACES.md § 4.4` or `Q-DD-04`.
    - **Sane file counts:** `find .planning/reconciliation -name '*.md' | wc -l` returns at least 55 (4 elexon-open + 22 elexon-wontfix + 33 entsoe-needs-info + 18 entsoe-open if surfaced + 5 entsog-open = ~82; lower allowed if the verifier surfaces fewer ENTSO-E nullability findings on the actual run, but the 55 floor catches the load-bearing baseline).
    - **Run report updated:** `grep -c '## Findings emitted' .planning/phases/07-reconciliation/07-02-VERIFICATION-REPORT.md` returns 1.
  </acceptance_criteria>
  <done>
    Every Drift instance the regenerated `vault-curl-schema-validation.md` surfaces has a triaged finding file under `.planning/reconciliation/<vendor>/<NN>-<slug>.md` with the correct `status:` and (where applicable) `reason:`. The 5 load-bearing examples from DRIFT-SURFACES.md are present. The 22 Elexon manual-transformer-schema slugs are each a wontfix-v3 finding. The 33 ENTSO-E 401s are each a needs-info-defer-entitlement finding. RECON-02 is satisfied.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live vendor APIs → verifier process | The 6 vendor APIs are the upstream trust source; the verifier makes ~150-200 HTTP calls to them with embedded API keys |
| Verifier process → `vault-curl-schema-validation.{json,md}` | The reports could in principle echo full curl commands including substituted API keys if the verifier logs them |
| `.planning/reconciliation/<vendor>/*.md` → git history | Findings are committed; any sensitive vendor info pasted into the body becomes durable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | I (Information disclosure) | `vault-curl-schema-validation.md` regenerated report | mitigate | After Task 1, the executor checks `grep -nE '<your-entsoe-api-key>|ENTSOE_API_KEY=[A-Za-z0-9-]{16,}' vault-curl-schema-validation.{md,json}` returns zero hits (the verifier substitutes placeholders for display but stores the substituted text — the existing behaviour at L160-173 of the original script does the right thing; this is a preservation check). If any actual key appears, that's a regression in 07-01 that 07-02 must surface. |
| T-07-02-02 | I (Information disclosure) | Finding markdown bodies | mitigate | The finding-file template's `## Context` section discusses Drift, not credentials. The triage walker pastes verifier-output excerpts but not full curl commands. Acceptance check: `grep -rE 'ENTSOE_API_KEY=[A-Za-z0-9-]{16,}|securityToken=[A-Za-z0-9-]{16,}' .planning/reconciliation/` returns zero hits. |
| T-07-02-03 | T (Tampering) | The 6 Vendor directories under `.planning/reconciliation/` | accept | These are committed markdown; tampering = git push surface, same as all other planning state. No new attack surface. |
| T-07-02-04 | D (Denial of service) | Vendor 5xx flakes during the verifier run | accept | If a vendor is flaky during the run (per drift research Risk #4 — energy-sector Christmas-day-style degenerate profiles), some 5xx may show up as findings. The triage default for 5xx is `open` (likely transient, re-verify in 07-03) per drift research; the 5 load-bearing acceptance findings are not 5xx-dependent, so the gate still holds. |

</threat_model>

<verification>

Plan-level verification (run after BOTH tasks land):

```bash
# 1. Verifier reports regenerated and current:
test -f "C:/Users/Bobbo/OneDrive/Desktop/Learning/AI/quant-vault/30-vendors/vault-curl-schema-validation.md"
head -5 "C:/Users/Bobbo/OneDrive/Desktop/Learning/AI/quant-vault/30-vendors/vault-curl-schema-validation.md" | grep -E '^Generated: 2026-05-1[89]|2026-05-2'   # later than baseline

# 2. Six Vendor directories exist:
for v in elexon entsoe entsog gie neso openmeteo ; do
  test -d ".planning/reconciliation/$v" || echo "MISSING $v"
done

# 3. Load-bearing acceptance gate (RECON-02 hard requirement):
ls .planning/reconciliation/elexon/ | grep -E 'ndf-schema-table-missing-published-at|ndfd-schema-table-missing-published-at|fuelhh-schema-table-missing-published-at' | wc -l   # >= 3
ls .planning/reconciliation/entsog/ | grep -E 'hydrogen|interruptions|methane|oxygen|physical-flows' | wc -l                                                                  # >= 5
grep -l 'reason: defer-entitlement' .planning/reconciliation/entsoe/*.md | wc -l                                                                                              # >= 33

# 4. Triage-label hygiene:
grep -hE '^status:' .planning/reconciliation/*/*.md | sort -u                          # only `open`, `wontfix`, `needs-info`
grep -hE '^reason:' .planning/reconciliation/*/*.md | sort -u                          # only `v3-candidate`, `defer-entitlement`

# 5. 22 Elexon wontfix-v3 cases all present:
for slug in agpt agws atl fou2t14d fuelinst imbalngc inddem indgen indo indod itsdo lolpdrm market_depth melngc netbsad nonbm remit soso temp tsdf tsdfd uou2t14d ; do
  grep -l "^dataset: $slug$" .planning/reconciliation/elexon/*.md > /dev/null || echo "MISSING wontfix-v3 for elexon/$slug"
done

# 6. Citation discipline:
grep -l 'reason: v3-candidate' .planning/reconciliation/*/*.md | xargs grep -L 'PROJECT.md § Out of Scope' | wc -l   # expect 0
grep -l 'reason: defer-entitlement' .planning/reconciliation/*/*.md | xargs grep -lE 'DRIFT-SURFACES.md § 4\\.4|Q-DD-04' | wc -l   # = count of needs-info findings
```

</verification>

<success_criteria>

RECON-02 from REQUIREMENTS.md is satisfied:
- `gridflow-drift-check` ran across all 6 Vendors and regenerated JSON + markdown reports
- Every Vendor under `.planning/reconciliation/<vendor>/` has a finding file for every Drift instance Verification surfaces
- Every finding carries a `status:` (`open` | `wontfix` | `needs-info`) and, where applicable, a `reason:` (`v3-candidate` | `defer-entitlement`)
- The 5 load-bearing findings from DRIFT-SURFACES.md §§ 4.1, 4.2, 4.4, 4.5, 4.6 ALL surface (acceptance gate)
- The 22 Elexon `manual_transformer_schema` slugs each have a `wontfix`/`v3-candidate` finding citing PROJECT.md § Out of Scope
- The 33 ENTSO-E HTTP 401 cases each have a `needs-info`/`defer-entitlement` finding citing DRIFT-SURFACES § 4.4 + Q-DD-04 + D-06

</success_criteria>

<output>
After both tasks complete, create `.planning/phases/07-reconciliation/07-02-SUMMARY.md` per the standard summary template. Capture:
- Per-Vendor finding counts (from the run report's `## Findings emitted` section)
- Confirmation that all 5 load-bearing acceptance checkboxes were ticked before Task 2 began
- Any 5xx vendor flakes encountered during the run
- Any deviations from the triage matrix (e.g. surfaced Drift that didn't fit any row → planner's discretion on triage; document the call)
- Conventional Commit hashes for each per-Vendor finding commit
- Pointer for 07-03: "Read `.planning/reconciliation/*/` files where `status: open` to find the work — there are <N> of them"
</output>
