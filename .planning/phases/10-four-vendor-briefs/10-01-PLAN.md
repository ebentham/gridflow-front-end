---
plan_id: 10-01
phase: 10
title: Four-vendor batch coverage (ENTSO-G 33 · GIE 8 · NESO 33 · Open-Meteo 6) + site-wide consistency — close the v2 milestone
status: ready_to_execute
autonomous: false
executor_model: opus
wave: 1
estimated_duration_hours: spans multiple sessions (manual Claude Design loop gates rendering)
expected_commits: 12-20
requirements:
  - ENTSOG-01
  - ENTSOG-02
  - ENTSOG-03
  - ENTSOG-04
  - GIE-01
  - GIE-02
  - GIE-03
  - GIE-04
  - NESO-01
  - NESO-02
  - NESO-03
  - NESO-04
  - METEO-01
  - METEO-02
  - METEO-03
  - METEO-04
  - SITE-01
  - SITE-02
depends_on: [Phase 7, Phase 8B, Phase 8C, Phase 8D, Phase 9]
context: 10-CONTEXT.md
handover: ../../handovers/2026-06-05-phase-10-claude-design-handover.md
references:
  - .planning/handovers/2026-05-27-phase-9-claude-design-handover.md
  - .planning/phases/09-entsoe-content-briefs/09-01-SUMMARY.md
  - content-briefs/{entsog,gie,neso,openmeteo}/_landing.md
  - content-briefs/{entsog,gie,neso,openmeteo}/<slug>.md (80 dataset briefs, all on disk)
  - authored-pages/entsoe/actual_generation.html (D-22 gold-standard reference)
  - site/hifi/data/entsoe.json (49-entry manifest — model for the 4 new manifests)
  - src/gridflow_front_end/build.py (REAL_VENDORS / COMING_SOON_VENDORS)
files_modified:
  - authored-pages/{entsog,gie,neso,openmeteo}/<slug>.html (76 or 80 new — Claude Design output, landed by orchestrator)
  - site/hifi/data/entsog.json (new)
  - site/hifi/data/gie.json (new)
  - site/hifi/data/neso.json (new)
  - site/hifi/data/openmeteo.json (new)
  - src/gridflow_front_end/build.py (4 vendors COMING_SOON_VENDORS → REAL_VENDORS)
  - site/hifi/index.html (stat strip count reconcile)
  - site/hifi/data-sources.html (catalog eyebrow + vendor-row links)
  - site/hifi/assets/site.js (footer count line, if present)
  - .planning/STATE.md / .planning/ROADMAP.md / .planning/REQUIREMENTS.md (Phase 10 close-out + count reconcile)
  - .planning/phases/10-four-vendor-briefs/10-01-SUMMARY.md (new — closure doc)
---

# Phase 10 / Plan 10-01 — Four-vendor batch coverage + site-wide consistency (v2 close-out)

## Goal

After this plan executes, the v2 milestone is **complete**:

1. All four remaining vendors (ENTSO-G, GIE, NESO, Open-Meteo) have full dataset documentation at `fuelhh` fidelity — every dataset page rendered (authored via Claude Design), every vendor hub real (not a coming-soon stub).
2. `src/gridflow_front_end/build.py` carries `entsog`, `gie`, `neso`, `openmeteo` in `REAL_VENDORS` (removed from `COMING_SOON_VENDORS`); the two-stub GIE split (`gie_agsi` + `gie_alsi`) is reconciled into a single `REAL_VENDORS["gie"]`.
3. Four manifests (`site/hifi/data/{entsog,gie,neso,openmeteo}.json`) list every dataset, grouped per the vendor taxonomy fixed in the `_landing` briefs.
4. Site-wide dataset count strings are **consistent and correct** across `index.html`, `data-sources.html`, and `site.js`; every vendor row on the catalog links to a real hub; zero coming-soon stub links remain.
5. All three CI gates green on the full six-vendor surface: `gridflow-build --check` idempotent, `htmlhint` 0 errors, `lychee --offline --include-fragments` 0 errors.
6. STATE / ROADMAP / REQUIREMENTS reconciled; `10-01-SUMMARY.md` written; PR opened to `main`.

## Provenance — where Phase 10 actually stands (disk truth, 2026-06-05)

The planning docs were written at different times and disagree. Disk is the authority:

| Step | Target | On disk | Status |
|---|---|---|---|
| Content briefs (80) | entsog 33 · gie 8 · neso 33 · openmeteo 6 | **all present** | ✅ DONE (ad-hoc, 4 parallel agents 2026-05-20; never tracked in a PLAN) |
| Vendor `_landing` briefs (4) | one per vendor | present | ✅ DONE (Phase 8D) |
| Authored dataset pages | 80 | **4** (one POC per vendor) | ❌ 76 (D-20) or 80 (D-22) remaining — see D-1 |
| Vendor manifests | 4 `.json` | 0 | ❌ T5 |
| build.py migration | 4 vendors | 0 (all `COMING_SOON`) | ❌ T6 |
| Site-wide count | 162 | partial (index says wrong "163") | ❌ T8 |
| Authored hub `data-root` | `"../"` | **already `"../"` on all 6 hubs** | ✅ already correct (T7 = verify-only) |

**The research-heavy step (briefs) is already complete.** Phase 10 is now rendering + wiring + counts, not content production. That is why this plan front-loads four decisions (T0) and treats T1–T4 as a manual Claude Design loop the user drives.

## Authoring path (locked 2026-06-05)

**Manual Claude Design**, same as Phase 9 — Claude Code (orchestrator) does **not** generate the dataset HTML directly. This respects the Phase 8B locked decision (AI-ported pages were "deemed sub-optimal in editorial quality" and archived). The orchestrator's role is: land Claude Design output → `gridflow-build` → spot-check against acceptance criteria → wire manifests + migration → verify CI. The paste-ready Claude Design prompts live in `.planning/handovers/phase-10/{entsog,gie,neso,openmeteo}-prompt.md`; the workflow + acceptance pack is `.planning/handovers/2026-06-05-phase-10-claude-design-handover.md`.

## Hard rules (carry from project working agreements; restated for the executor)

- **Never auto-commit.** The user commits (overrides global "commit after each passing test" — a docs site has no test rhythm). Stage + show + ask.
- **Never push** without explicit user ack. Branch off `main` (do NOT work on `main`).
- Do NOT touch `authored-pages/elexon/fuelhh.html` or `authored-pages/elexon/system_prices.html` (sacred visual references — byte-identical invariant).
- Conventional commits only (`feat:` / `fix:` / `docs:` / `chore:` / `refactor:`); one concern per commit.
- No SaaS framing, no fake-live indicators, no emojis in shipped files ("born honest" rule).
- Do NOT fabricate data — sample rows are verbatim from briefs; if a source is absent, the page omits the section (no invented codelists/schemas).
- `uv` only (never bare `pip`); Python 3.11+; the dev server stays stdlib-only (Jinja2 is `[build]` extras).

---

## Task 00 — Resolve four open decisions (USER GATE)

<wave>1</wave>
<autonomous>false</autonomous>

**RESOLVED 2026-06-05** (user "go ahead" on recommended answers):

| # | Decision | Ruling |
|---|---|---|
| D-1 | Overview/Caveats fidelity | **D-22 — synthesise** `#overview` + render `#caveats`, matching Phase 9. **Scope = 80 pages** (the 4 D-20 POCs are re-rendered for consistency). Off-ramp: if lean is preferred later, strip the synthesis lines → 76 pages. |
| D-2 | Site-wide dataset count | **162** (empirical: `vault/neso` = 33 `.md`, no README). Correct REQUIREMENTS (NESO 34→33), ROADMAP, SITE-01 (163→162), and the `index.html` "163 ingested" shipped by `0d683ce`, in Task 08. |
| D-3 | GIE hub shape | **Unified `gie`** (one hub, AGSI + ALSI as two groups). Both `gie_agsi` + `gie_alsi` COMING_SOON stubs removed in Task 06. |
| D-4 | Reference-page strategy | **`authored-pages/entsoe/actual_generation.html` as the shared D-22 structural reference** for all four vendors + each vendor's POC page as the vendor-specific layout example. |

The original decision text is kept below for the record.

---

This task is a **human decision checkpoint**. Nothing downstream is correct until these four are settled. Present them, capture answers, record in this plan + STATE.md, then proceed.

### D-1 — Overview/Caveats fidelity: D-22 (synthesised) or D-20 (lean)?

The 80 Phase 10 dataset briefs are **D-20 vintage** (no `# Overview`; caveats are research-record, not a rendered section) — verified: `grep -l "^# Overview" content-briefs/{entsog,gie,neso,openmeteo}/*.md` returns nothing. Two paths:

- **(a) D-22 — synthesise (RECOMMENDED).** Match Phase 9 (ENTSO-E): Claude Design synthesises a 3-paragraph `#overview` and renders a `#caveats` section from the brief, exactly as `authored-pages/entsoe/actual_generation.html` does. PR #19 merging is implicit acceptance of this richer standard. Keeps all five non-Elexon vendors visually consistent.
  - **Coupling (do not miss):** if D-22 is the standard, the **4 existing POC pages are D-20 and become inconsistent** → they must be re-rendered. **Scope = 80 pages, not 76.**
- **(b) D-20 — lean.** Honour the original 5-dataset POC approval (user stripped Overview/Caveats as "redundant/verbose" on 2026-05-20). No synthesis section. **Scope = 76 pages** (the 4 POCs already conform).

Default to **(a)**. The handover prompts are authored for (a); a one-line note in each prompt says how to strip to (b).

### D-2 — Site-wide dataset count: 162 (empirical) vs 163 (legacy docs)

`ls vault/neso/*.md | wc -l` = **33** (no README in the vendored snapshot). Real total = 33+49+33+8+33+6 = **162**. But REQUIREMENTS `NESO-01/02/03` say "34", `SITE-01` says "163", and commit `0d683ce` already shipped "**163** Datasets ingested" into `index.html`. Recommend: **adopt 162**, update REQUIREMENTS/ROADMAP/SITE-01 + the index stat strip + pillar prose + `data-sources.html` eyebrow accordingly. (If the user knows of a 163rd dataset not in the vendored vault, surface it before T8.)

### D-3 — GIE hub shape: unified `gie` vs split `gie_agsi` + `gie_alsi`

`COMING_SOON_VENDORS` splits GIE into `gie_agsi` (L134) + `gie_alsi` (L148); Phase 8D authored a **single** `gie` hub brief (AGSI + ALSI as two internal groups) and the deploy-fix already writes a unified `data-sources/gie.html`. Recommend: **unified `gie`** in `REAL_VENDORS` (one hub, two groups), both stubs removed. GIE-02/GIE-04 explicitly allow this.

### D-4 — Reference-page strategy for Claude Design

Recommend: **`authored-pages/entsoe/actual_generation.html` is the PRIMARY structural/visual reference for all four vendors** (it is the only D-22 page; mimic its structure, classes, inline styles — **not** its content), with **each vendor's own POC page attached as a secondary vendor-specific layout example** (carries the dual-host BASE URL cell for gie/openmeteo, the JSON/no-schema schema table for entsog, etc.). This avoids upgrading 4 POC pages first and maximises cross-vendor consistency.

<acceptance_criteria>
- Each of D-1..D-4 has a recorded answer in this section and in STATE.md "Decisions Locked (v2)".
- Page-count scope (76 or 80) is fixed for T1–T4.
- The site-wide count target (162 or 163) is fixed for T8.
</acceptance_criteria>

---

## Tasks 01–04 — Render + land authored dataset pages (manual Claude Design loop)

<wave>2</wave>
<autonomous>false</autonomous>
<depends_on>[Task 00]</depends_on>

Tasks 01–04 are **one per vendor**, each driven by the manual Claude Design loop. The orchestrator does not generate HTML; it lands what Claude Design returns. Per-vendor batch orders, the paste-ready prompt, and the per-page acceptance checklist are in the handover pack.

**Shared per-task loop (per the handover doc):**

1. User opens a fresh Claude Design conversation, pastes `.planning/handovers/phase-10/<vendor>-prompt.md`, attaches `authored-pages/entsoe/actual_generation.html` + the vendor POC page + the batch's briefs, asks for the pages.
2. User pastes returned HTML to the orchestrator: "land these."
3. Orchestrator saves each to `authored-pages/<vendor>/<slug>.html`, runs `gridflow-build` (while the vendor is still `COMING_SOON`, authored dataset pages are copied to `data-sources/<vendor>/<slug>.html` by `copy_authored_dataset_pages_for_coming_soon()`; `build_dataset_stubs_from_landings` stops emitting a stub for any slug that now has an authored page).
4. Orchestrator spot-checks each page against the acceptance checklist (body attribute contract, asset paths `../../assets/`, breadcrumb, 6-cell hero, 4-cell stats, sidebar siblings, chart type ∈ {stackedArea, sparkline, barsH, priceLadder}, schema table with citations, verbatim sample rows, no emojis/live-wording, both inline scripts). On failure: push back in the Claude Design conversation, do **not** paper over it.
5. Orchestrator stages + shows the diff; **user commits** (`feat(<vendor>): Claude Design pages — batch N`).

A vendor's Task is complete when all its briefs have a landed, acceptance-passing authored page.

> **Hub `#about` prerequisite (found in the entsog proof batch, 2026-06-05).** Every D-22 dataset page's `#caveats` intro links to the vendor hub's `#about` anchor ("vendor-wide caveats") — the established Elexon/ENTSO-E pattern. The four Phase-10 hubs (authored in 8D) **lack `#about`**, so the first D-22 batch per vendor fails `lychee --include-fragments` until it's added. Fix: add a `<section class="block" id="about">` of 3–4 vendor-wide caveats (lifted from the vendor's `_landing.md` `# Caveats`) to `authored-pages/<vendor>/_landing.html`, mirroring the entsoe hub. **Done: entsog. Pending: gie, neso, openmeteo** — add each before/with that vendor's first D-22 batch. (elexon + entsoe already have it.)
>
> **Do NOT re-render hubs via Claude Design.** The 8D-authored hubs are correct (depth-1 `data-root="../"`, `../assets/`). Claude Design re-rendered the entsog hub with depth-2 paths (`data-root="../../"`) in the proof zip — a GitHub-Pages-breaking regression. Hubs are maintained by the orchestrator (only `#about` additions); Claude Design produces **dataset pages only**.

### Task 01 — ENTSO-G (33 datasets, 5 groups)
- Prompt: `phase-10/entsog-prompt.md`. POC already present: `aggregated_physical_flows.html` (re-render under D-1=(a)). New: 32 (or 33 if D-1=(a)).
- Vendor specifics the prompt encodes: **JSON** (not XML); `timeZone=UCT` quirk; HTTP-404-empty convention; **mostly no Pydantic schema** (1/33 typed — `EntsogPhysicalFlow`; the rest derive columns dynamically via `GenericEntsogJsonTransformer`, schema table cites `silver/entsog/generic.py`); queried per operator-point-direction at GB interconnection points (Bacton IUK/BBL, Moffat).
- Suggested batches: (1) Zone + core Operational flows — `aggregated_physical_flows`, `physical_flows`, `nominations`, `renominations`, `allocations`; (2) Capacities — `firm_*`, `interruptible_*`, `available_through_*` (10); (3) Quality + CMP + Tariff — `gcv`, `wobbe_index`, `methane/hydrogen/oxygen_content`, `cmp_*`, `tariffs`, `tariff_simulations`; (4) Reference + UMM — `connection_points`, `operators`, `balancing_zones`, `operator_point_directions`, `interconnections`, `aggregate_interconnections`, `interruptions`, `urgent_market_messages`.

### Task 02 — GIE (8 datasets, 2 groups: AGSI + ALSI)
- Prompt: `phase-10/gie-prompt.md`. POC: `storage.html`. New: 7 (or 8 under D-1=(a)).
- Vendor specifics: **JSON**; `x-key` header API key (free registration — the only authenticated vendor in the batch); gas-day 06:00 UTC; `GasStorage` (storage, storage_reports) + `LNGTerminal` (lng) typed, 5 reference/news datasets dynamic (`AgsiJsonTransformer`); dual-host BASE URL cell (`agsi.gie.eu` / `alsi.gie.eu`); queried per country (AGSI 9, ALSI 8).
- Suggested batches: (1) Storage core — `storage`, `storage_reports`, `unavailability`, `lng`; (2) Reference + news — `about_listing`, `about_summary`, `news`, `news_item`.

### Task 03 — NESO (33 datasets, 4 groups)
- Prompt: `phase-10/neso-prompt.md`. POC: `carbon_intensity.html`. New: 32 (or 33 under D-1=(a)).
- Vendor specifics: **JSON REST**; no auth; 30-min settlement; gCO₂/kWh; **schemas exist** (`schemas/neso.py`: `CarbonIntensity`, `CarbonIntensityStats`, `CarbonIntensityFactor`, `GenerationMix`, `RegionalIntensity`) **but transformers are dynamically generated** via `register_neso_transformers()` — schema table cites `schemas/neso.py` line numbers; "Generation Mix" + "Regional" carry a vendor `beta` flag; queried per settlement-period / date / region-id / postcode.
- Suggested batches: (1) Carbon Intensity · National (10); (2) Statistics (2) + Generation Mix (3); (3) Regional A — `regional_current/england/scotland/wales/postcode/regionid/intensity` + fw24h set (9); (4) Regional B — fw48h + pt24h + postcode/regionid range variants (9).

### Task 04 — Open-Meteo (6 datasets, 2 groups: Forecast + Historical)
- Prompt: `phase-10/openmeteo-prompt.md`. POC: `forecast_solar.html`. New: 5 (or 6 under D-1=(a)).
- Vendor specifics: **JSON columnar**; no auth; **dual base-URL** (`archive-api.open-meteo.com` historical vs `api.open-meteo.com` forecast — not a versioning split); **schemas in `weather.py` NOT `openmeteo.py`** (`DemandWeather`, `WindWeather`, `SolarWeather`); three-form naming asymmetry (`open-meteo` vault / `openmeteo` slug / `open_meteo` registry); ERA5 ~5-day lag; queried per site (7 demand / 12 wind / 6 solar locations).
- Suggested batches: (1) Forecast — `forecast_demand`, `forecast_solar`, `forecast_wind`; (2) Historical — `historical_demand`, `historical_solar`, `historical_wind`.

<acceptance_criteria>
- Per vendor: `ls authored-pages/<vendor>/*.html | grep -v _landing | wc -l` equals the vendor's dataset count (33 / 8 / 33 / 6).
- Every landed page passes the handover acceptance checklist (orchestrator-verified).
- `gridflow-build` exits 0 after each landing batch; no slug has both an authored page and a coming-soon stub.
</acceptance_criteria>

---

## Task 05 — Author four vendor manifests

<wave>3</wave>
<autonomous>true</autonomous>
<depends_on>[Task 01, Task 02, Task 03, Task 04]</depends_on>

<read_first>
- site/hifi/data/entsoe.json (49-entry, 9-group model — copy its shape exactly)
- content-briefs/{entsog,gie,neso,openmeteo}/_landing.md (the `# Groups` sections are the canonical taxonomy + per-dataset rows)
- src/gridflow_front_end/build.py (manifest schema the build expects — `groups`, dataset `id`/`title`/`freq`/`lag`/`rows`)
</read_first>

<action>
Author one manifest per vendor, group structure lifted verbatim from each `_landing.md` `# Groups`:

- `site/hifi/data/entsog.json` — 33 datasets, **5 groups**: Operational · Point Data (19) · Zone Data (1) · CMP Data (3) · Tariff Data (2) · Reference · Network Topology (8).
- `site/hifi/data/gie.json` — 8 datasets, **2 groups**: Storage (AGSI) (7) · LNG (ALSI) (1). (Unified `gie`, per D-3.)
- `site/hifi/data/neso.json` — 33 datasets, **4 groups**: Carbon Intensity · National (10) · Statistics · National (2) · Generation Mix · National (3) · Carbon Intensity · Regional (18).
- `site/hifi/data/openmeteo.json` — 6 datasets, **2 groups**: Forecast (3) · Historical (3).

Each dataset entry mirrors the `entsoe.json` field set (`id`, `title`, `freq`, `lag`, `rows`, group membership). Per-dataset `freq`/`lag`/`rows` come from the `_landing` group tables. Validate each JSON parses and the dataset count matches the vendor total.
</action>

<acceptance_criteria>
- `python -c "import json; [json.load(open(f'site/hifi/data/{v}.json')) for v in ('entsog','gie','neso','openmeteo')]"` exits 0.
- Dataset-entry counts: entsog 33, gie 8, neso 33, openmeteo 6.
- Group counts: entsog 5, gie 2, neso 4, openmeteo 2.
</acceptance_criteria>

---

## Task 06 — Migrate four vendors COMING_SOON_VENDORS → REAL_VENDORS

<wave>3</wave>
<autonomous>true</autonomous>
<depends_on>[Task 05]</depends_on>

> **Sequencing constraint (why migrate last, per advisor):** a `REAL_VENDORS` vendor renders its dataset pages via the manifest — authored page if present, else **templated fallback from vault**. The dataset template has only ever been proven on Elexon; Phase 9 used authored pages for ENTSO-E and never exercised the template on a non-Elexon vendor. So migrating a vendor **before** its authored pages are complete risks `gridflow-build` emitting broken/ugly templated pages and breaking CI. **Migrate a vendor only after its Task 01–04 authored set is 100% landed.** Per-vendor migration (entsog when entsog is done, etc.) keeps the site shippable throughout; do not batch-migrate all four while any vendor is partial.

<read_first>
- src/gridflow_front_end/build.py (REAL_VENDORS dict ~L60; COMING_SOON_VENDORS list ~L118; the `gie_agsi`/`gie_alsi`/`gie` handling ~L817-842)
- content-briefs/{entsog,gie,neso,openmeteo}/_landing.md (the `# Source map` rows define each vendor's `vendor_meta`: label, doc_base, lede, region, domain)
</read_first>

<action>
For each vendor whose authored pages are complete:
1. Add a `REAL_VENDORS["<vendor>"]` entry with full `vendor_meta` (label, `vendor_doc_base`, lede, region, domain) sourced from the `_landing` brief.
2. Remove its `COMING_SOON_VENDORS` entry. For GIE: remove **both** `gie_agsi` and `gie_alsi`, add a single `REAL_VENDORS["gie"]` (per D-3); reconcile the `gie` alias handling in `build_coming_soon_stubs` / `_vendor_stub_metadata` so the unified hub still resolves.
3. Run `gridflow-build` → confirm the vendor hub renders from the authored `_landing.html` (or `vendor-hub.html.j2` over the manifest) rather than `vendor-coming-soon.html.j2`, and all dataset pages render (authored).
4. `grep -n COMING_SOON_VENDORS src/gridflow_front_end/build.py` must show none of `entsog`/`gie_agsi`/`gie_alsi`/`gie`/`neso`/`openmeteo` remaining once all four are migrated.
</action>

<acceptance_criteria>
- `grep -nE "entsog|gie_agsi|gie_alsi|neso|openmeteo" src/gridflow_front_end/build.py` shows these keys only under `REAL_VENDORS`, never `COMING_SOON_VENDORS`.
- `gridflow-build` exits 0; `/data-sources/{entsog,gie,neso,openmeteo}.html` each render as real hubs.
- `gridflow-build --check` idempotent across the full surface.
</acceptance_criteria>

---

## Task 07 — Verify authored-hub `data-root` (verify-only; the bug is already fixed)

<wave>3</wave>
<autonomous>true</autonomous>

The carried-over STATE.md note ("authored vendor-hub `data-root='../../'` → `'../'`") is **stale** — as of 2026-06-05 all six authored hubs already carry `data-root="../"`. This task is a guard, not a fix.

<action>
`grep -n 'data-root' authored-pages/*/_landing.html` — confirm all six (elexon, entsoe, entsog, gie, neso, openmeteo) read `data-root="../"`. If any Claude Design output for a NEW hub (none expected — hubs were authored in 8D) regresses to `../../`, fix to `../` (depth-1 hubs only — do **not** touch dataset pages, whose `data-root="../../"` is correct at depth-2).
</action>

<acceptance_criteria>
- `grep -c 'data-root="../"' authored-pages/*/_landing.html` returns 1 for each of the 6 hub files.
- `grep -rl 'data-root="../../"' authored-pages/*/_landing.html` returns nothing.
</acceptance_criteria>

---

## Task 08 — Site-wide count consistency + catalog real-hub links (SITE-01, SITE-02)

<wave>3</wave>
<autonomous>true</autonomous>
<depends_on>[Task 06]</depends_on>

<read_first>
- site/hifi/index.html (stat strip ~L376-397; pillar-2 prose ~L443 — both touched by 0d683ce)
- site/hifi/data-sources.html (eyebrow ~L26; per-category chip counts ~L37-42; vendor rows + per-section headers)
- site/hifi/assets/site.js (footer count line, if present)
</read_first>

<action>
Apply the D-2 count (recommended **162**) consistently:
1. `index.html` stat strip — reconcile the "163 Datasets ingested" cell shipped by `0d683ce` to the D-2 figure; re-check the pillar-2 prose for the same number.
2. `data-sources.html` — eyebrow ("Catalog · 6 vendors · 163 datasets ingested · 33 documented at full fidelity" → D-2 figure + "**all** documented at full fidelity" once the four vendors are real); per-section dataset counts (Electricity/Gas/LNG/Weather/Carbon headers) recomputed; **every vendor row links to a real `/data-sources/<vendor>.html` hub** with zero coming-soon stub links.
3. `site.js` footer line (if it carries a dataset total) — reconcile to D-2.
4. `grep -rn "33 Elexon" site/hifi/` returns only the on-purpose Elexon-specific catalog chip (not a stale total); `grep -rn "163\|34 datasets\|5 planned\|7 vendors" site/hifi/` returns zero stale hits.
</action>

<acceptance_criteria>
- The D-2 count appears identically in `index.html`, `data-sources.html`, and `site.js` (if applicable); no other site-wide total appears.
- `grep -rnE 'data-sources/[a-z_]+\.html' site/hifi/data-sources.html` shows every vendor row → real hub; zero links into a `*-coming-soon` shape.
- SITE-01 + SITE-02 success-criteria greps from ROADMAP Phase 10 SC#5 pass.
</acceptance_criteria>

---

## Task 09 — CI gates + planning reconcile + SUMMARY + PR

<wave>4</wave>
<autonomous>false</autonomous>
<depends_on>[Task 05, Task 06, Task 07, Task 08]</depends_on>

<action>
1. **Cold-rebuild CI parity** (the 30-second pre-push sanity check):
   ```bash
   rm -rf site/hifi/data-sources && gridflow-build && gridflow-build --check
   htmlhint --config .htmlhintrc 'site/hifi/**/*.html'
   lychee --no-progress --offline --include-fragments './site/hifi/**/*.html'
   ```
   All three exit 0. Counts: 162 dataset pages + 6 real hubs, idempotent.
2. **Reconcile planning to disk truth:** update `REQUIREMENTS.md` (NESO 34→33, SITE-01 163→162), `ROADMAP.md` (Phase 10 `[ ]`→`[x]`, progress table, NESO/total counts), `STATE.md` (Phase 10 complete, v2 milestone done; record D-1..D-4 resolutions).
3. **Write `10-01-SUMMARY.md`** (mirror `09-01-SUMMARY.md`): outcome, per-vendor table (datasets / groups / authored-page count / D-22-or-D-20 / commit range), CI evidence, decisions D-1..D-4 as locked, count reconcile note, carried-forward items (none expected — this closes v2).
4. **Branch + commits + PR:** branch `feat/phase-10-four-vendor` off `main`; atomic conventional commits (per-vendor render batches as `feat(<vendor>):`, manifests + migration as `feat(build):`, counts as `fix(site):`, planning as `docs:`); **stage + show + ask the user before every commit and before push**; open PR to `main`. Post-merge the GitHub Pages deploy fires the same three gates — that is the true CI.
</action>

<acceptance_criteria>
- All three CI gates exit 0 on a cold rebuild.
- ROADMAP Phase 10 row `[x]`; REQUIREMENTS ENTSOG/GIE/NESO/METEO/SITE all `delivered`; STATE shows v2 complete.
- `10-01-SUMMARY.md` exists with the per-vendor table.
- PR open to `main`; v2 milestone closeable via `/gsd-complete-milestone` afterward.
</acceptance_criteria>

---

## Wave / dependency summary

```
T00 (decisions — USER GATE)
  └── T01 entsog ┐
      T02 gie    │  (manual Claude Design loop — parallelisable across vendors;
      T03 neso   │   each is independent — disjoint authored-pages/<vendor>/ paths)
      T04 meteo  ┘
            └── T05 manifests ──┐
                                ├── T06 migrate (per-vendor, only when that vendor's pages are 100% landed)
                T07 verify hubs ┤
                                └── T08 counts ── T09 CI + reconcile + SUMMARY + PR (USER GATE: commit/push)
```

**Parallelisation:** T01–T04 are independent (disjoint paths) and gate only on T00. The manual Claude Design loop is the real-world rate limiter, not compute. T05/T07 can start as soon as their inputs exist; T06 is strictly per-vendor-after-render; T08 after T06; T09 closes.

## Out of scope (explicit)

- Direct AI generation of dataset HTML (rejected — Phase 8B locked decision; manual Claude Design only).
- Retrofitting Elexon (D-22 already applied) or ENTSO-E (Phase 9) pages.
- Pydantic-class drift fixes for the schema-absent entsog/gie datasets (v3 candidate per locked decision #8/#11).
- Live-API verification for any vendor (gridflow connector code is canonical; charts are illustrative snapshots).

---
*Plan 10-01 created 2026-06-05. The four content-brief sets (80 briefs) and four `_landing` briefs already exist on disk; this plan covers rendering (manual Claude Design), wiring (manifests + REAL_VENDORS migration), and site-wide consistency to close the v2 milestone. Handover pack: `.planning/handovers/2026-06-05-phase-10-claude-design-handover.md` + `phase-10/<vendor>-prompt.md`.*
